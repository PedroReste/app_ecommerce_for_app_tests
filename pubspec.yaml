name: shopflow
description: Prototype e-commerce app for tagging automation testing
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^13.0.0
  provider: ^6.1.1
  cached_network_image: ^3.3.1
  uuid: ^4.3.3

flutter:
  uses-material-design: true
